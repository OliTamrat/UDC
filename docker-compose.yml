services:
  dashboard:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/udc-water.db
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
